package cat.copernic.AnnaSerrano.EntreBicisAnna.Entity;

/**
 * Enum que representa els possibles estats d’una ruta.
 *
 * - PENDENT: la ruta està en curs o encara no ha estat finalitzada.
 * - FINALITZADA: la ruta s’ha completat i es pot validar.
 */
public enum EstatRutaType {
    PENDENT, FINALITZADA
}
